<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/ex"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ex"
        android:supportsRtl="true"
        android:name=".parseServer"
        android:theme="@style/AppTheme">

        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".MainActivityStart"></activity>
        <activity android:name=".ProductPage"></activity>
        <activity android:name=".PasswordChangeActivity"></activity>
        <activity android:name=".ProfileSettings" />
        <activity android:name=".ui.chat.ChatModule"/>
    </application>

</manifest>